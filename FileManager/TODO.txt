 
 
 
 
 
 
 ToDo: 210810
 1. 下载/上传界面手动选择路径
	1.1 根据config 确定 下载/上传 默认路径选择
 2. 获取目录超时处理
 3. 小文件压入流式传输
 4. 可变长byte[]数组
 5. 身份验证
 6. SocketFactory 中维护 SocketClient池
 7. Server 端翻译路径请求

 8. Server端建立并维护 SocketSession返回SessionId至Client (超时处理)
    SocketFactory 中维护SessionId
    总是报"索引超出数组界限", 尝试在更内部捕捉异常
    
 9. TransferInfo 里面的priority不好用, 用一个EmergentList代替
    保存时, 先整体保存, 不考虑bytes bias的优化
 TODO :

    连接建立的第一个包 (0 for length) 1-255 bytes 描述sessionId
    256-511 bytes 描述 keys

